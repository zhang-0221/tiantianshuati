{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "tauri": "tauri",
    "desktop:prepare": "node scripts/build-desktop-assets.mjs",
    "desktop:build": "npm run desktop:prepare && tauri build --config src-tauri/tauri.conf.json",
    "mobile:build": "node scripts/build-android.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "@vercel/kv": "^3.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
