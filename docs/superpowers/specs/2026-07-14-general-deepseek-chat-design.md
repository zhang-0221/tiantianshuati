# 通用 DeepSeek 聊天设计

## 目标

将内置聊天从受限的学习问答助手调整为通用 DeepSeek 对话入口，使其能自然地完成解释、分析、代码与长回答。

## 改动

- 用中性系统提示替换“简洁、引导式、学习资料相关”的限制。
- 保留中文优先和安全、诚实的基础约束。
- 将 `max_tokens` 从 1024 调整为 4096。
- 保留 `deepseek-chat`、已有 API Key、20 轮对话历史、现有聊天面板和错误处理。

## 验证

- 静态测试确认通用系统提示与 4096 token 限制。
- 运行完整测试套件，确认其他功能不受影响。
